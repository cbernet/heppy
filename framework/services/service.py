class Service(object):
    
    def start(self):
        '''Start the service.
        Must be called by the framework, not the user.
        '''
        pass
    
    def stop(self):
        '''Stop the service.
        Must be called by the framework, not the user.
        '''
        pass 
